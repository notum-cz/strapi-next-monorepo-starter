{
  "copy": [
    "apps/strapi/.env",
    "apps/ui/.env.local",
    ".env"
  ],
  "symlink": [],
  "postSetup": [
    "pnpm install --frozen-lockfile --prefer-offline",
    "pnpm --filter @repo/shared-data --filter @repo/design-system build"
  ]
}
