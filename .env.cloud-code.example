# ============================================================================
# Cloud Code Configuration (Optional - Stub Only)
# ============================================================================
#
# This file contains environment variables for the optional cloud-code service.
# Copy these to your .env.local file to configure cloud-code.
#
# IMPORTANT: Cloud-code is currently stub-only (no backend implemented).
# By default, CLOUD_CODE_ENABLED=false, so the build will never break.
#
# ============================================================================

# Feature Flag: Enable/disable cloud-code (default: false)
CLOUD_CODE_ENABLED=false

# Cloud Code Service Endpoint (required when enabled)
# This should point to your cloud-code execution service
CLOUD_CODE_ENDPOINT=https://cloud-code.example.com

# API Key for Authentication (required when enabled)
# Get this from your cloud-code service dashboard
CLOUD_CODE_API_KEY=your-api-key-here

# Request Timeout in milliseconds (default: 30000)
# Maximum time to wait for function execution
CLOUD_CODE_TIMEOUT=30000

# Retry Attempts (default: 3)
# Number of times to retry failed requests
CLOUD_CODE_RETRY_ATTEMPTS=3

# ============================================================================
# Usage Examples
# ============================================================================
#
# Execute a function:
#   import { executeFunction } from '@repo/cloud-code';
#
#   const result = await executeFunction({
#     functionName: 'processPayment',
#     parameters: { amount: 100, currency: 'USD' },
#   });
#
# Batch execution:
#   import { executeBatch } from '@repo/cloud-code';
#
#   const results = await executeBatch([
#     { functionName: 'sendEmail', parameters: { to: 'user@example.com' } },
#     { functionName: 'logEvent', parameters: { event: 'user_signup' } },
#   ]);
#
# Health check:
#   import { healthCheck } from '@repo/cloud-code';
#
#   const health = await healthCheck();
#   console.log('Healthy:', health.healthy);
#
# ============================================================================
# Current Status
# ============================================================================
#
# Cloud-code is currently a STUB IMPLEMENTATION. All operations return
# stub responses. This allows you to:
#
# 1. Integrate cloud-code calls into your application now
# 2. Test the integration without a backend
# 3. Implement the backend later without changing client code
# 4. Keep the build working even when cloud-code is "enabled"
#
# When you're ready to implement the backend:
# 1. Deploy your cloud-code execution service
# 2. Set CLOUD_CODE_ENDPOINT to your service URL
# 3. Set CLOUD_CODE_API_KEY with your API key
# 4. Update packages/cloud-code/src/client.ts to make real HTTP calls
# 5. Enable with CLOUD_CODE_ENABLED=true
#
# ============================================================================
