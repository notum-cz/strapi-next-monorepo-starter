/**
 * Strapi seed migration - Creates initial data
 * Run with: pnpm strapi migrate:latest
 */

"use strict";

module.exports = {
  async up() {
    // This migration will be used to seed initial data
    // Note: Strapi migrations are for database schema changes
    // For data seeding, use the seed.py script instead
  },

  async down() {
    // Rollback logic if needed
  },
};
