build:
  docker:
    web: Dockerfile
  # Passing Heroku Config vars to Docker as build args
  # These values will be baked into the image at build time
  # Use them only for static configuration and compile-time toggles
  # args:
  #   APP_PUBLIC_URL: ${APP_PUBLIC_URL}
  #   STRAPI_URL: ${STRAPI_URL}
  #   STRAPI_REST_READONLY_API_KEY: ${STRAPI_REST_READONLY_API_KEY}
