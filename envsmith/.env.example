# EnvSmith Application Environment Variables
# Copy to .env.local for development

# Optional: Default Vercel team slug for smoke tests
VERCEL_TEAM_SLUG=jeremy-bowers-s-projects

# Optional: Default Vercel project name for smoke tests
VERCEL_PROJECT_NAME=strapi-template-new-world-kids

# Required for Vercel sync: Add your Vercel token
# Get from: https://vercel.com/account/tokens
# VERCEL_TOKEN=your_vercel_token_here

# Required for GitHub sync: Users will provide PAT via UI
# This file shows the app's own env structure, not user secrets
